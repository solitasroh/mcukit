# Prompt — retro cycle15 (after)

`learnings.json` v1.0, 5 entries. Run `/retro feature-name` after Cycle 1.5. Output must include
Phase 0 trend delta table, banned-vocab compliance, and locked-vocab exemption respected.
