# Expected — code-review cycle15 (before)

All specialists run regardless of past hit rate. No "자동 생략됨" line. Same finding from prior
review re-emitted as a fresh issue (no fingerprint-based suppression). No reference to NEVER_GATE
or 100 entries 윈도우.
