# Prompt — code-review cycle15 (before)

User runs `/code-review src/lib/foo.js` with rkit v0.9.13 (no adaptive gating, no cross-review
dedup, no NEVER_GATE concept). All specialists are dispatched on every review.
