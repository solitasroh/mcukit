# Expected — investigate cycle15 (after)

🛑 멈춤: "lib/context를 (A) 책임 단위 4모듈 분할인지 (B) 단일 facade 유지 + 내부 함수 그룹화인지 결정해야 합니다."

| 선택지 | ✅ 좋은 점 | ❌ 나쁜 점 |
|---|---|---|
| A) 책임 단위 4모듈 분할 | ✅ 외부 호출자가 필요한 모듈만 import 가능, 의존 표면 축소.<br>✅ 단일 책임 원칙 준수 — 향후 변경 영향 범위가 좁아짐. | ❌ 사이클 1에서 정리한 11 keys facade 호환성 영향, 호출자 grep 필요. |
| B) 단일 facade 유지 + 내부 함수 그룹화 | ✅ 외부 호출자 영향 0, facade 11 keys 그대로 유지.<br>✅ 변경 한 파일에 집중, diff 명확. | ❌ 모듈 경계가 코드 주석 수준으로만 명시되어 책임 분리가 약함. |

추천안: B — 사이클 1에서 facade를 11 keys로 정리한 직후라 외부 호환성 위험을 최소화하는 게 사이클 1.5 정책("회귀 표면 최소화")과 일관. 책임 분리는 다음 사이클에서 평가.
